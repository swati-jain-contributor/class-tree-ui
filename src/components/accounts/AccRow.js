import React, {PropTypes} from 'react';

class AccRow extends React.Component {
  constructor(props, context) {
    super(props, context);
    this.showDetail = this.showDetail.bind(this);
    this.state = {
    };
  }

  showDetail() {
  }
  componentDidUpdate() {
  }

  render() {
    return ({

    }
    );
  }
}
export default AccRow;
