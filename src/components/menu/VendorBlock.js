import React, {PropTypes} from 'react';

const VendorBlock = ({}) => {
  return (
    <div className="vendor-block  view-holder">
      <h3>VendorBlock</h3>
    </div>
  );
};

VendorBlock.propTypes = {
};

export default VendorBlock;
