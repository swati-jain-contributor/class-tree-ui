import React, {PropTypes} from 'react';

const AccountBlock = ({}) => {
  return (
    <div className="acc-block  view-holder">
      <h3>ColBlock</h3>
    </div>
  );
};

AccountBlock.propTypes = {
};

export default AccountBlock;
